\documentclass[11pt]{article}

% basic packages
\usepackage[margin=1in]{geometry}
\usepackage[pdftex]{graphicx}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{custom}
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage{float}
\usepackage{braket}
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,       % False for boxes, True for colored text
    linkcolor=blue,        % Color for internal links (sections, equations)
    urlcolor=cyan,         % Color for external websites
    citecolor=red          % Color for citations (\cite)
}
% page formatting
\usepackage{fancyhdr}
\pagestyle{fancy}

\renewcommand{\sectionmark}[1]{\markright{\textsf{\arabic{section}. #1}}}
\renewcommand{\subsectionmark}[1]{}
\lhead{\textbf{\thepage} \ \ \nouppercase{\rightmark}}
\chead{}
\rhead{}
\lfoot{}
\cfoot{}
\rfoot{}
\setlength{\headheight}{14pt}

\linespread{1.03} % give a little extra room
\setlength{\parindent}{0.2in} % reduce paragraph indent a bit
\setcounter{secnumdepth}{2} % no numbered subsubsections
\setcounter{tocdepth}{2} % no subsubsections in ToC

\begin{document}

% make title page
\thispagestyle{empty}
\bigskip \
\vspace{0.1cm}

\begin{center}
{\fontsize{22}{22} \selectfont Lecture Notes on}
\vskip 16pt
{\fontsize{36}{36} \selectfont \bf \sffamily Machine Learning Theory}
\vskip 24pt
{\fontsize{18}{18} \selectfont \rmfamily Owen Lin} 
\vskip 6pt
{\fontsize{14}{14} \selectfont \ttfamily owen2006lin@gmail.com} 
\vskip 24pt
\end{center}

{\parindent0pt \baselineskip=15.5pt
These notes are for a graduate level course on machine learning theory, with an 
emphasis on statistical learning theory and online learning.
\begin{itemize}
    \item Shalev-Schwartz and Ben-David: \textit{Understanding Machine Learning: From Theory to Algorithms}
    \item Blum, Hopcroft, Kannan: \textit{Foundations of Data Science}
\end{itemize}

}
 


% make table of contents
\newpage
\microtoc
\newpage

% main content
\section{Basics?}
\subsection{Premise of Learning}
To begin, we'll present some illustrative examples. Suppose we desire to train a model to classify spam e-mails.
\begin{itemize}
    \item A naive approach is to simply memorize all previous e-mails that have been classified as spam by a human user.
    Then, when a new email arrives, we search for this e-mail in the dataset of all past e-mails and classify accordingly. 
    A problem with this ``\textbf{learning by memorization}'' is that this doesn't generalize well do data outside of the original 
    dataset.

    \item A better approach is to look at the previous set of all emails, an then extract a set of words that correlates strongly
    with classification. Then we apply this rule for all future e-mails. ``\textbf{Inductive reasoning}'' allows us to generalize to data
    outside of the original dataset.

    \item Inductive reasoning may lead us to false conclusions. An example is the \textbf{Pigeon Superstition}. The experiment placed food
    at random intervals initially. Pigeons associated whatever action they did when food arrived and engaged in that activity more.
    Over time, they began to associate the action with the food delivery even though the true pattern was random.

\end{itemize}

\noindent The goal of course, is to develop performant learners. Human learners can filter out this pigeon superstition from common sense,
and we'd desire principles that allow a learner to reject useless conclusions. One powerful tool is the incorporation of \textbf{prior knowledge}
to bias the learning mechanism. The idea is to develop domain specific knowledge, and then translate it into a way to bias the learning
to agree. 


\subsection{Empirical Risk Minimization}
First, we'll lay down some formal notation for learning. In the classical setting:
\begin{itemize}
    \item The learner has access to several datasets for inputs. The \textbf{domain set} \( X \) contains the set of objects to label. Each point may be
    represented using a vector of features. The \textbf{label set} \( y \) contains all possible labels, for now we'll restrict it to a two element set: \( y = \{-1, +1\}   \). 
    Finally, the \textbf{training data} \( S \) consists of a set of \( (x_{m}, y_{m}) \) pairs, the training examples the learner has access to. Namely, 
    \( S = ((x_{1}, y_{1}) \ldots (x_{m}, y_{m})) \).
 
    \item The learner should output a prediction rule \( h \colon X \to y \) that can be used to predict the label of domain points. The function is also known as a 
    \textbf{hypothesis} or a \textbf{predictor}. We also write this hypothesis of some output of our learning algorithm \( A \) upon receiving training sample \( S \).

    \item To generate the data, we sample some points in \( X \) with probability distribution \( D \). Then, we use the true labeling function \( f \colon X \to y \),
    and create labels \( y_{i} = f(x_{i}) \).

    \item The error \( L \) of a classifier is the probability it does not predict the correct label of a random data point over \( D \). Namely,
    
    \[ 
        L_{D, f}(h) \equiv \mathbb{P}_{x \sim D}[h(x) \neq f(x)] \equiv D(\{x: h(x) \neq f(x)\}) 
    \]
    \( L \) is also called the \textbf{generalization error} or the \textbf{risk} or the \textbf{true error}. 
    How we use the distribution D is, if we have some subset \( A \subset X\) then \( D(A) \) gives us how likely it is to observe a point 
    \( x \in A \). 

\end{itemize}

\noindent A good learner should try to minimize the error with respect to some unknown \( D \) and truth \( f \). Because the learner doesn't know \( D \) and \( f \),
we can use a heuristic for the loss called the \textbf{training error}:

\[ 
    L_{S}(h) \equiv \frac{|\{i \in [m] : h(x_{i}) \neq y_{i}\} |}{m} 
\]

\noindent This paradigm, in finding a predictor \( h \) to minimize the training error is \textbf{Empirical Risk Minimization}.


\begin{itemize}
    \item Overfitting is obviously a big problem. A hypothesis that works very well on the training set can generalize very poorly 
    over all data. Additionally, there may exist multiple possible hypothesis that perform equally on the training set, and choosing one is unclear.
    \item A solution is to restrict ERM over a finite set of hypotheses, also called the \textbf{hypothesis class} \( h \in H \). This is a form of 
    inductive bias, but is not always sufficient to guarantee we don't overfit. 
    \item The \textbf{Realizability Assumption} states there exists \( h* \in H \) such that \( L_{(D,f)}(h*) = 0 \). More formally, we say that a distribution \( D \)
    is realizable with respect to hypothesis class \( H \) if the there exists a perfect hypothesis with no true error. In other words, the solution for our distribution
    belongs to our hypothesis class.
    \item The \textbf{i.i.d assumption} assumes the examles in the training set are independently and identically distributed. We denote \( S \sim  D^{m} \)
    where \( D^{m} \) is the probability over m-tuples by applying \( D \) to pick each element of the tuple independently.
\end{itemize}

\noindent The process for empirical risk minimization is as follows: we've narrowed down our set of hypotheses to a hypothesis class \( H \). We want to minimize the empirical
risk over a training set \( S \), and can choose to assume the realizability assumption. If we do, then ERM selects any hypothesis \( h \) that achieves 0 training error. Note 
that if we don't assume the realizability assumption, it's not guaranteed that this will be the case, leading us into \textbf{Agnostic Learning}.
\\

\noindent Some important metrics to keep in mind are \textbf{confidence} and \textbf{accuracy}. We can denote the probabiltiy of sampling a training set is not 
representative of the underlying \( D \) using the confidence \( (1 - \delta) \). The accuracy parameter can be denoted as \( \epsilon \), where we note a training failure
when \( L_{(D,f)}(h_{S}) \geq \epsilon \)

\begin{itemize}
    \item We'd like to upper bound the probability that some sample will lead to learning failure. Namely, we want to find the bound for
    
    \[ 
        D^{m}(\{S|_{x} \colon L_{(D,f)}(h_{s}) > \epsilon\} ) 
    \]
    \item By the realizability assumption, ERM gives us a hypothesis \( h_{s} \) such that \( L_{S}(h_{s}) = 0\), so the only time learning fails is when 
    our sample is in the set of misleading samples, where there exists hypothesis with 0 training error, but nonzero generalization error. The set of all such samples is also called \( M \), so
    we wish to solve 
    
    \[ 
        D^{m}(\{S|_{x} \colon L_{(D,f)}(h_{s}) > \epsilon\} )  \leq D^{m}(M)
    \]

    \item Since each element is sampled i.i.d, the probability a sampled point is one where a bad hypothesis looks correct is \( 1 - L_{(D,f)}(h) \), which is less than \( 1 - \epsilon \). If we note
    that \( 1 - \epsilon \leq e^{-\epsilon} \) and note that each element is independent, then multiplying the probability over all points in the training set, the probability that a bad hypothesis leads to failure
    over a sample is 
    \[ 
        D^{m}(\{S|_{x} \colon L_{s}(h) = 0\}) \leq (1-\epsilon)^{m} \leq e^{-\epsilon m} 
    \]

    Applying the union bound inequality, we have 
    \[ 
        D^{m}(\{S|_{x} \colon L_{(D,f)}(h_{s}) > \epsilon\} ) \leq |H|e^{\epsilon m}
    \]

    \item For a finite hypothesis class, if we have an integer \( m \) such that \( m\geq\frac{\log{\left( |H|/\delta \right)}}{\epsilon} \), then for any labeling function \( f \) and distribution \( D \)
    where the realizability assumption holds, we have probability of at least \( 1 - \delta \) that we have a bad hypothesis:
    
    \[ 
        Pr_{S\sim D^{m}}[L_{(D,f)}(h_{S}) \leq \epsilon] \geq 1-\delta 
    \]

    In other words, for sufficiently large m, the ERM rule over a finite hypothesis class will be probably (with confidence \( 1-\delta \)) approximately (up to error \( \epsilon \)) correct.

\end{itemize}

\subsection{PAC Learning}
In this section, we concern ourselves with hypotheses that are \textbf{P}robably \textbf{A}pproximately \textbf{C}orrect. 
\\

\noindent We say a hypothesis class \( H \) is \textbf{PAC learnable} if there exists a function \( m_{H} : (0,1)^{2} \to \mathbb{N} \) and a learning algorithm obeying:
 \( \forall \epsilon,\delta \in (0,1) \), for every distribution \( D \) over \( X \) and for every labeling function \( f : X \to \{0,1\}   \), if the realizability
 assumption holds w.r.t. \( H, D, f \), then when A is given \( m \geq m_{H}(\epsilon, \delta) \) i.i.d examples drawn from \( D \) and labeled with f, it returns a hypothesis
 \( h \) satisfying 
 \[ 
     err_{D,f}(h) \leq \epsilon 
 \]
with probability at least \( 1-\delta \) over random training sample. we call \( m_{H} \) the \textbf{sample complexity} of learning \( H \), basically it specifies how many training examples
are sufficient to guarantee a PAC solution. Some things to note:
\begin{itemize}
    \item You may wonder why an algorithm can't just find the perfect solution each time if it belongs to the hypothesis class anyways. Because the training set is randomly generated, it might not
    be representative of the distribution \( D \). For example, a case could be that the same point is simply resampled over and over again. 
    \item Even if the sample is representative of \( D \), the size is finite and less than the whole domain. Thus, there may be some details of \( D \) that simply can't be captured by the sample. We
    introduce accuracy paramter \( \epsilon \) to allow for these minor mistakes. 
    \item If we assume that \( S \) is perfect and of good size, we're still not guaranteed to find the best hypothesis, since our learning algorithm might not be deterministic. Thus, we introduce the 
    confidence parameter \( \delta \) to describe how likely we're able to reach the target accuracy.
\end{itemize}

\noindent If we note the theorem from above, every finite hypothesis class is actually PAC learnable, with sample complexity  
\[ 
    m_{H}(\epsilon, \delta) \leq \left[\log{\left( \frac{(|H|/\delta)}{\epsilon} \right)}\right] 
\]
\\

\noindent \textbf{Example} Consider the class of all one-dimensional threshholds. Let the domain be \( \mathbb{R} \), and restrict our hypothesis class \( H \) to be all thresholds of form 

\[ 
    h_{a}(x) =  
    \begin{cases}
        1, &\text{if } x \leq a\\
        0, &\text{if } x \geq a    
    \end{cases}
\]

\noindent Assume the realizable setting, and assume that our learned threshold \( \hat{a} \) is less than the true threshold \( a^{*} \). Choose a point \( a_{L} \) to the left of \( a^{{*}} \) such that
the probability mass of the interval \( (a_{L}, a^{*}] \) is exactly \( \epsilon \). If our algorithm picks a threshold \( \hat{a} \) to the left of \( a_{L} \), then the generalization error will be greater than
\( \epsilon \). Now for this to happen, all \( m \) training samples must  must miss this interval, because if one fell in the correct interval, ERM would automatically select \( \hat{a} \) to fall under the \( < \epsilon \) 
range. The probability this happens is \( (1-\epsilon)^{m} \). Applying this logic to the other side, we have \( \mathbb{P}(L_{D}(h_{\hat{a}})) \leq 2(1-\epsilon)^{m} \). If we let this failure probability be at most 
\( \delta \), then rearanging gives us

\[ 
    m \geq \frac{1}{\epsilon}\ln{\left( \frac{2}{\delta} \right)} 
\]
\\

\noindent \textbf{Example}: For a finite hypothesis class, we've shown that the upper bound of the probability that some sample will fail is 
\( \delta \geq |H|e^{-m\epsilon} \). Rearranging, the lower bound for the sample size needed to achieve \( \epsilon \) error with probability \( 1-\delta \) is 

\[ 
    m \geq \frac{1}{\epsilon}\left(\ln{\left( |H| \right)} + \ln{\left( \frac{1}{\delta} \right)}\right) 
\] 
\\

\subsection{Sample Complexity}

\noindent The \textbf{sample complexity} \( m \) is the number of training data examples an algorithm needs to successfully learn a target function.
Note that for an infinite hypothesis class size, these bounds that we found for the sample complexity are vacuous. In this case, we instead focus on the number of ways to label samples rather than on the size of a hypothesis class. 
\\

\noindent We define the \textbf{Growth Function} as follows: For an unlabled sample \( T = \{x_{1}, x_{2}, \ldots x_{n}\}  \), let \( H[T] \) denote the set of all ways (sequences) to label our sample using hypotheses from \( H \). 
Namely, \( H[T] = \{h(x_{1}), \ldots h(x_{n}) : h \in H\}  \). Then we define the growth function \( \Pi_{H}(n) \) as 

\[ 
    \Pi_{H}(n) = max_{T \in X^{n}} |H[T]| 
\]
\\
\noindent Colloquially, it's the number of ways to labe an n size sample using hypotheses from class \( H \). For binary classification, the max is trivially \( 2^{n} \). 
\\

\noindent \textbf{Example}: for the class of one dimensional thresholds, the growth function is \( n+1 \). This is because to choose a labeling, we must choose a point \( a^{*} \) such that any points left of this 
is 0, and any point to the right is 1. For n points, there are \( n + 1 \) spaces we can choose for \( a^{*} \). 
\\

\noindent We can use the growth function as a replacement for the size of our hypothesis class. Any hypothesis class is PAC learnable if the sample complexity satisfies

\[ 
    m\geq\frac{2}{\epsilon}\left(\log_{2}{\left( 2\Pi_{H}(2m) \right)} + \log_{2}{\left( \frac{1}{\delta} \right)}\right) 
\]
\\

\noindent The proof is quite long, and linked \href{https://www.cs.umd.edu/~hanshao/848p/3-scub.pdf}{here}. Some clever tricks to take away:
\begin{itemize}
    \item We can upper bound the probability that Bernoulli random variables using \textbf{Chernoff Bound}. If we let \( X_{1},\ldots,X_{m} \) be a sequence of Bernoulli random variables with expectation \( \mu \),
    the probability they are far from the mean is
    
    \[ 
        \text{Pr}\left[\frac{1}{m}\sum_{k=1}^{m}X_{k} \leq \frac{\mu}{2}\right] \leq \text{exp}\left(-\frac{m\mu}{8}\right)
    \]

    Meanwhile, for any \( \epsilon > 0 \), \textbf{Hoeffding's bound} gives us
    
    \[ 
        \text{Pr}\left[\left\lvert\frac{1}{m}\sum_{k=1}^{m}X_{i} - \mu \right\rvert \geq \epsilon \right] \leq 2\text{exp}(-2m\epsilon^{2}) 
    \]

    \item Using a \textbf{ghost sample}. Still don't understand this one\ldots

\end{itemize}



\end{document}